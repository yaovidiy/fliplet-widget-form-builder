<template v-for="(option, index) in options">
  <div class="radio radio-icon">
    <input type="radio" :id="name + '-' + index" :name="name" v-model="value" :required="required" :value="option.id || option.label" v-on:change="updateValue()" >
    <label :for="name + '-' + index">
      <span class="check"><i class="fa fa-circle"></i></span> \{{ option.label || option.id }}
    </label>
  </div>
</template>
