<template v-for="(option, index) in options">
  <div class="radio radio-icon">
    <input type="radio" :id="formId + name + '-' + index" :name="name" v-model="value" :required="required" :value="option.id" v-on:change="updateValue()" >
    <label :for="formId + name + '-' + index">
      <span class="check"><i class="fa fa-circle"></i></span> \{{ option.label || option.id }}
    </label>
  </div>
</template>
